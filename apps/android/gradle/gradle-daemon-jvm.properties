# Keep Gradle's jlink behavior consistent across Android Studio, the terminal, and CI.
# In particular, do not select the locally installed GraalVM 17 for Android's JDK image transform.
toolchainVersion=21
toolchainVendor=ORACLE
